{
  "name": "webext-ui-port",
  "version": "0.0.1",
  "description": "A general purpose UI port (between background and e.g. popup) for use in web extensions.",
  "author": "Joost Lubach",
  "private": false,
  "license": "MIT",
  "files": [
    "dist"
  ],
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "check": "tsc -p . --noEmit",
    "lint": "eslint src --ext ts,tsx",
    "test": "NODE_ENV=test DATA_ENV=test jest"
  },
  "peerDependencies": {
    "wxt": "^0.20.6"
  },
  "devDependencies": {
    "@types/webextension-polyfill": "^0.12.3",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "webextension-polyfill": "^0.12.0",
    "ytil": "workspace:^"
  }
}